<ci:TriggerSettingsControlBase x:TypeArguments="triggers:PreTimePointTriggerSettings"
                               x:Class="YcyzClass.Controls.TriggerSettingsControls.PreTimePointTriggerSettingsControl"
                               xmlns="https://github.com/avaloniaui"
                               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                               xmlns:ci="http://ycyzclass.tech/schemas/xaml/core"
                               xmlns:local="clr-namespace:YcyzClass.Controls.TriggerSettingsControls"

                               xmlns:triggers="clr-namespace:YcyzClass.Models.Automation.Triggers">
    <StackPanel DataContext="{Binding $parent[local:PreTimePointTriggerSettingsControl]}"
                Spacing="4"
                Orientation="Horizontal">
        <ComboBox VerticalAlignment="Center" x:Name="StateComboBox" SelectionChanged="StateComboBox_SelectionChanged">
            <ComboBoxItem>
                上课
            </ComboBoxItem>
            <ComboBoxItem>
                课间休息
            </ComboBoxItem>
            <ComboBoxItem>
                放学
            </ComboBoxItem>
        </ComboBox>
        <ci:Field Prefix="之前" Suffix="秒">
            <NumericUpDown Value="{Binding $parent[local:PreTimePointTriggerSettingsControl].Settings.TimeSeconds, Converter={x:Static ci:NotNaNConverter.Instance}}" />
        </ci:Field>
    </StackPanel>
</ci:TriggerSettingsControlBase>
